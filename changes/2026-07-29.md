# Changes made on 2026-07-29

## Backlog audit

- Re-audited every unchecked item in `TODO.md` against the current source,
  focused tests, runtime artifacts, and verification record.
- Split broad harness, macOS, packaging, first-run, and governance entries so
  completed local work is visible without claiming unfinished release
  acceptance criteria.
- Kept four-player minigame voting, the full live lifecycle matrix, activity
  coverage, quest polish, Linux, CI, rights clearance, production
  authentication, and public operations open.
- Clarified that several non-trolley activities already have ordinary
  leave/disconnect paths; the remaining work is policy consistency and live
  lifecycle validation.

## P1 minigame correctness

- Fixed Photo Fun server score ordering so the final available photograph is
  recorded and broadcast before the film-out check can end the game.
- Preserves multiple assignment scores from the same final shutter and rejects
  additional score submissions after the client reports film exhaustion.
- Corrected Toon Escape's `DistributedTwoDGameAI` initialization guard so the
  attribute it sets is the same attribute it checks.
- Removed three unreachable Trolley Tracks place-decider result branches that
  contained `TODO NAME` and referenced an unqualified symbol. The server emits
  only the retained vote and random result reasons.

## Verification

- `game/tools/test_target_photo_cleanup.py`: 14 tests passed, including final
  photo scoring and post-limit rejection.
- `game/tools/test_diving_twod_cleanup.py`: 10 tests passed, including the
  exact Toon Escape guard and all three Trolley Tracks result sources.
- The full focused P0/P1 set passed 75 tests on the local custom macOS Panda
  runtime.
- Parsed all 1,860 game Python files successfully after the fixes.
- Existing user changes in `LocalAvatar.py`,
  `DistributedTutorialInterior.py`, `TutorialManager.py`, `__builtins__.pyi`,
  and `typings/` were not modified.

## macOS evidence boundary

- The local Apple Silicon runtime is arm64 CPython 3.9.25 with Panda3D 1.11.0
  and importable `panda3d.otp`/`panda3d.toontown` extensions.
- The local arm64 Astron build, setup check, source-launcher validation, and a
  recorded client login/world-entry/normal-exit smoke test passed.
- These ignored native artifacts are local development builds, not
  distributable releases: they retain absolute Homebrew dynamic-library
  paths, lack reproducible build provenance, and have not passed signing,
  notarization, or clean-machine installation.
- Linux native runtime/server work remains unstarted.

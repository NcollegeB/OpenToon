# 2026-07-28

## Cross-platform neutral resource overlay

- Traced the missing macOS presentation changes to the repository boundary:
  `game/resources` is an ignored, separate checkout of the pinned upstream
  resource snapshot. The earlier Windows PNG, DNA, and BAM working-tree edits
  were documented but were never part of the OpenToon Git history.
- Added a tracked, deterministic Panda3D-based generator at
  `game/otp/otpbase/NeutralResources.py`.
- The generator creates `game/open_town_assets` without modifying or
  redistributing the upstream archive.
- Added original neutral artwork for both active Gag Shop signs and five
  neighborhood destination signs.
- Replaced character-branded endpoints in all 16 English street-map textures.
- Generated neutral star overrides for the seven destination portrait models
  and neutral planter overrides for the two displayed landmark model paths.
- Generated both Acorn entrance variants without their displayed character
  subtree while retaining all 147 collision solids across three collision
  nodes.
- Rewrote targeted player-visible DNA titles and sign baselines while
  preserving internal group names, codes, filenames, and model identifiers.
- Added `open_town_assets` before `resources` on Panda3D's model path.
- Windows and macOS/Linux setup now build and verify the overlay. The source
  client also recreates it when it is missing.
- Verified 63 generated overlay files, parsed all 26 changed DNA files, and
  confirmed live VFS resolution chooses the overlay.

This remains a presentation-layer compatibility pass. The separately
downloaded upstream archive still contains inactive legacy resources and is
not represented as rights-cleared for redistribution.
